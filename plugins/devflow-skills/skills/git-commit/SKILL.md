---
name: git-commit
description: "检查 Git 变更与项目提交约定，生成提交信息，或在用户明确要求时执行提交。生成信息不等于授权暂存或提交。"
---

# Git 提交

围绕用户授权的变更完成提交，保留项目既有规范。单独调用 `$git-commit` 视为要求提交；用户要求“生成/建议提交信息”时只读分析并输出信息，不暂存、不提交。

中英文请求均按同一语义处理：`draft/suggest a commit message` 只生成信息，`commit these changes` 表示执行提交。回复优先遵循用户明确的语言要求及适用语言约定，否则沿用当前对话的主要语言；不要把 skill 正文或默认提示词的语言当作用户偏好。

## 检查本次范围

- 查看 `git status -sb`、工作区和暂存区 diff、近期提交；先列文件再按需读内容，私有文件不直接完整打印。没有可提交变更时说明即可，不询问是否继续空操作。
- 读取项目规范中的提交约定与实际 commitlint 配置；配置优先于历史惯例。动态 scope 按项目生成逻辑解析，不假设目录名或 package name 就是合法值。
- 检查本次代码是否让项目规范过期；需要时更新相关段落，并把对应共享规范、项目入口及必要忽略规则一并纳入提交。只读生成信息时只指出遗漏，不自动改文档；没有规范不为提交强制初始化。
- 提交内容遵循全局与项目的本地私有信息边界。检查实际暂存内容，不能仅凭文件名、`.gitignore` 或 `dev-only` 判断安全；已跟踪文件即使被忽略仍会进入提交。
- 只选择当前任务相关文件或 hunks，保留用户无关的工作区和暂存区内容。发现已暂存的无关变更时，不直接运行会包含整个 index 的普通 commit；不能可靠隔离时说明具体范围并询问。
- 新建共享规范可能未被跟踪或被忽略，需明确检查；按 `project-conventions` 的 Git 规则解决被忽略的共享规范，不用 `git add -f`。`.task` 沿用项目版本控制策略，不强行提交本地记录。

## 生成信息与提交

1. 按项目约定选 type / scope；没有约定时用 `<type>(<scope>): <subject>`，scope 可省略。常用 `feat`、`fix`、`refactor`、`docs`、`chore`，按实际行为选择，不追求凑齐类别。
2. 提交信息语言依次遵循用户明确要求（含 `zh` / `en` 参数）、项目提交约定、近期提交的稳定语言；仍无依据时沿用当前对话语言。subject 说明具体改动，长度与允许值以项目校验为准，不添加 AI 标记或共同作者。
3. 只生成信息时到此结束。执行提交时精确暂存已确认范围，复查 staged diff 与私有信息，再创建提交；不自动 push 或 amend。
4. hook 失败先看错误，仅修正本次范围内的问题，再检查 hook 是否改动了文件并复核暂存内容后重试。不跳过 hook，不为通过提交顺手改无关代码；涉及新增范围时说明阻塞。
5. 用最新 commit 的文件清单和 `git status -sb` 确认结果，核实应提交的共享规范确实在 commit 内；检查无关暂存内容仍保留。

只生成信息时输出提交信息。实际提交后简要给出 commit ID、提交信息和验证结果；有相关文档未进入提交时说明原因，不把本地更新描述为已提交。
