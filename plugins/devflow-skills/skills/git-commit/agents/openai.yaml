interface:
  display_name: "Git 提交 · Git Commit"
  short_description: "检查变更并按授权提交 / Review changes and commit when requested"
  default_prompt: "使用 $git-commit 处理本次提交请求，沿用当前对话语言与项目约定；只要求生成提交信息时保持只读。"
